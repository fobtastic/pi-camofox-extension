# Security

## Credentials
- Do not commit `.env` or `.env.local` files.
- Pass proxy credentials through environment variables.
- This repository only includes `.env.example` placeholders.

## Reporting
If you find a security issue or accidental secret exposure, open a private report with the maintainers and rotate the affected credentials immediately.
